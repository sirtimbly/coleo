/**
 * IMAP Server Module
 * 
 * Provides IMAP4rev1 access to Octopai's Maildir storage,
 * allowing users to read brain/arm communications with any email client.
 */

export { ImapServer, startImapServer, type ImapServerConfig } from "./server";
