/**
 * Discovery Tools
 * 
 * MCP tools for reporting and managing discoveries.
 */

import { z } from "zod";
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";

/**
 * Register discovery-related tools on the MCP server
 */
export function registerDiscoveryTools(_server: McpServer): void {
	// Stub implementation - tools to be extracted from server.ts
}
