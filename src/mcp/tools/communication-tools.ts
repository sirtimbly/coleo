/**
 * Communication Tools
 * 
 * MCP tools for arm communication and coordination.
 */

import { z } from "zod";
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";

/**
 * Register communication-related tools on the MCP server
 */
export function registerCommunicationTools(_server: McpServer): void {
	// Stub implementation - tools to be extracted from server.ts
}
