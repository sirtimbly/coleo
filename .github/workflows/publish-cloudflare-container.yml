name: Publish Cloudflare container

on:
  push:
    branches:
      - master
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: publish-cloudflare-container-${{ github.ref }}
  cancel-in-progress: true

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.6"

      - name: Build control image
        run: docker build --platform linux/amd64 --file Dockerfile.cloudflare-control --tag coleo-control:${GITHUB_SHA} .

      - name: Build arm-agent image
        run: docker build --platform linux/amd64 --file Dockerfile.cloudflare-agent --tag coleo-agent:${GITHUB_SHA} .

      - name: Push immutable images
        run: |
          bunx wrangler@4.110.0 containers push coleo-control:${GITHUB_SHA}
          bunx wrangler@4.110.0 containers push coleo-agent:${GITHUB_SHA}
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Push latest image
        if: github.ref == 'refs/heads/master'
        run: |
          docker tag coleo-control:${GITHUB_SHA} coleo-control:latest
          docker tag coleo-agent:${GITHUB_SHA} coleo-agent:latest
          bunx wrangler@4.110.0 containers push coleo-control:latest
          bunx wrangler@4.110.0 containers push coleo-agent:latest
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
